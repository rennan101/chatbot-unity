// ==========================================================
// ÍCONES SVG GLOBAIS
// ==========================================================
export const SVG_CHECK = `<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"></polyline></svg>`;
export const SVG_SETTINGS = `<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="3"></circle><path d="M19.4 15a1.65 1.65 0 0 0 .33 1.82l.06.06a2 2 0 0 1 0 2.83 2 2 0 0 1-2.83 0l-.06-.06a1.65 1.65 0 0 0-1.82-.33 1.65 1.65 0 0 0-1 1.51V21a2 2 0 0 1-2 2 2 2 0 0 1-2-2v-.09A1.65 1.65 0 0 0 9 19.4a1.65 1.65 0 0 0-1.82.33l-.06.06a2 2 0 0 1-2.83 0 2 2 0 0 1 0-2.83l.06.06a1.65 1.65 0 0 0 .33-1.82 1.65 1.65 0 0 0-1.51-1H3a2 2 0 0 1-2-2 2 2 0 0 1 2-2h.09A1.65 1.65 0 0 0 4.6 9a1.65 1.65 0 0 0-.33-1.82l-.06-.06a2 2 0 0 1 0-2.83 2 2 0 0 1 2.83 0l.06.06a1.65 1.65 0 0 0 1.82.33H9a1.65 1.65 0 0 0 1-1.51V3a2 2 0 0 1 2-2 2 2 0 0 1 2 2v.09a1.65 1.65 0 0 0 1 1.51 1.65 1.65 0 0 0 1.82-.33l.06-.06a2 2 0 0 1 2.83 0 2 2 0 0 1 0 2.83l-.06.06a1.65 1.65 0 0 0-.33 1.82V9a1.65 1.65 0 0 0 1.51 1H21a2 2 0 0 1 2 2 2 2 0 0 1-2 2h-.09a1.65 1.65 0 0 0-1.51 1z"></path></svg>`;
export const SVG_DOWNLOAD = `<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"></path><polyline points="7 10 12 15 17 10"></polyline><line x1="12" y1="15" x2="12" y2="3"></line></svg>`;
export const SVG_FOLDER = `<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M22 19a2 2 0 0 1-2 2H4a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h5l2 3h9a2 2 0 0 1 2 2z"></path></svg>`;
export const SVG_SAVE = `<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M19 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h11l5 5v11a2 2 0 0 1-2 2z"></path><polyline points="17 21 17 13 7 13 7 21"></polyline><polyline points="7 3 7 8 15 8"></polyline></svg>`;
export const SVG_EDIT = `<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M11 4H4a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7"></path><path d="M18.5 2.5a2.121 2.121 0 0 1 3 3L12 15l-4 1 1-4 9.5-9.5z"></path></svg>`;
export const SVG_ARCHIVE = `<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="21 8 21 21 3 21 3 8"></polyline><rect x="1" y="3" width="22" height="5"></rect><line x1="10" y1="12" x2="14" y2="12"></line></svg>`;
export const SVG_FILE = `<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M13 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V9z"></path><polyline points="13 2 13 9 20 9"></polyline></svg>`;
export const SVG_TRASH = `<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="3 6 5 6 21 6"></polyline><path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2"></path></svg>`;
export const SVG_WARN = `<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M10.29 3.86L1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0z"></path><line x1="12" y1="9" x2="12" y2="13"></line><line x1="12" y1="17" x2="12.01" y2="17"></line></svg>`;
export const SVG_CLOCK = `<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"></circle><polyline points="12 6 12 12 16 14"></polyline></svg>`;
export const SVG_SPINNER = `<svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="12" y1="2" x2="12" y2="6"></line><line x1="12" y1="18" x2="12" y2="22"></line><line x1="4.93" y1="4.93" x2="7.76" y2="7.76"></line><line x1="16.24" y1="16.24" x2="19.07" y2="19.07"></line><line x1="2" y1="12" x2="6" y2="12"></line><line x1="18" y1="12" x2="22" y2="12"></line><line x1="4.93" y1="19.07" x2="7.76" y2="16.24"></line><line x1="16.24" y1="7.76" x2="19.07" y2="4.93"></line></svg>`;
export const SVG_COPY = `<svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="9" y="9" width="13" height="13" rx="2" ry="2"></rect><path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"></path></svg>`;
export const SVG_SHARE = `<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"></path><circle cx="9" cy="7" r="4"></circle><path d="M23 21v-2a4 4 0 0 0-3-3.87"></path><path d="M16 3.13a4 4 0 0 1 0 7.75"></path></svg>`;
export const SVG_EYE = `<svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"></path><circle cx="12" cy="12" r="3"></circle></svg>`;
export const SVG_EYE_OFF = `<svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M17.94 17.94A10.07 10.07 0 0 1 12 20c-7 0-11-8-11-8a18.45 18.45 0 0 1 5.06-5.94M9.9 4.24A9.12 9.12 0 0 1 12 4c7 0 11 8 11 8a18.5 18.5 0 0 1-2.16 3.19m-6.72-1.07a3 3 0 1 1-4.24-4.24"></path><line x1="1" y1="1" x2="23" y2="23"></line></svg>`;

// ==========================================================
// BANCO DE FEEDBACKS GAMERS (MEMES)
// ==========================================================
export const memesLoading = [
    "Farmando aura...", "Consultando o ancião...", "Esfregando a lâmpada...", "Calculando a física do Unity...", 
    "Procurando o ponto e vírgula perdido...", "Compilando os shaders...", "Perguntando pro StackOverflow...", 
    "Mineirando redstone para ligar o servidor...", "Carregando a barra de mana...", "Treinando a rede neural com café...", 
    "Girando a manivela do backend...", "Invocando os deuses do C#...", "Dando um git pull na sabedoria...", 
    "Baixando mais RAM...", "Refatorando o universo...", "Carregando novo isekai...", "Alimentando o tinhoso...", 
    "Fugindo de gremlins...", "Arregaçando goblins...", "Procurando uma elfa curandeira...", 
    "Entrando no Eva...", "Sincronizando com a Matrix...", "Ajustando o brilho dos óculos..."
];
export const memesSucesso = [
    "GG WP! Tudo salvo.", "É TETRA! Operação concluída.", "Dropou o loot lendário!", "Missão Cumprida (+100 XP).", 
    "Vitória Royale!", "Código buildado com zero warnings.", "Receba! Tudo certo por aqui.", 
    "Mais liso que rodar a 144fps.", "Pode ir pro abraço, tá salvo!", "Famoso 'toca pro pai'.", 
    "Novo membro na party!", "CTRL+C perfeito, patrão.", "Tá no pente!", "Deu bom! Pode favoritar.", 
    "Sincronizado perfeitamente.", "S-Rank! Operação perfeita.", "Bankai! Sucesso absoluto.", 
    "A Waifu notou você! Tudo certo.", "Conseguiu o final verdadeiro!", "Hackerman completou a ação.", 
    "Nem precisou do poder da amizade para dar certo."
];
export const memesErro = [
    "O servidor foi de base... F no chat.", "Tankou não. Tente novamente.", "Deu tela azul aqui, chefe.", 
    "Erro 404: Vontade de trabalhar sumiu.", "Eita, o Unity crashou (de novo).", "Fomos nerfados! Limite atingido.", 
    "Faltou poção de mana pra essa ação.", "Alguém tropeçou no cabo do servidor.", "NullReferenceException na vida real.", 
    "O estagiário apagou o banco de dados.", "Uma van branca (Truck-kun) bateu em você, tente novamente mais tarde.", 
    "Góticas no caminho de casa, a requisição falhou.", "Shinji não entrou no robô...", 
    "Omedetou... falhou miseravelmente.", "O poder de luta foi menor que 8000.", "Você foi pego num Genjutsu de erro.", 
    "Seu código foi de Isekai."
];
export const memesAviso = [
    "Calma lá emocionade, digita algo!", "Vai mandar o vazio pro além?", "Você não tem level suficiente pra isso.", 
    "Opa, tá tentando bugar a Matrix?", "Segura a emoção, o parceiro tá digitando...", "Miss click? Ação cancelada.", 
    "Inventário cheio! Limite atingido.", "Permissão negada. Você não é o dono!", "Esqueceu a chave da API em casa?", 
    "Hackerman detectado!", "B-baka! Faça direito!", "O sentido aranha está apitando... ação inválida.", 
    "Você ativou minha carta armadilha! (Bloqueado)", "Apenas usuários com Stand podem fazer isso."
];

export function getMeme(tipo) {
    let lista = [];
    if(tipo === 'loading') lista = memesLoading;
    else if(tipo === 'sucesso') lista = memesSucesso;
    else if(tipo === 'erro') lista = memesErro;
    else if(tipo === 'aviso') lista = memesAviso;
    return lista[Math.floor(Math.random() * lista.length)];
}

// ==========================================================
// FORMATADORES E MEMÓRIA CACHE (Sync Global de Nomes)
// ==========================================================
window.mapUsuarios = {}; 

export function formatarNomeUsuario(emailOrName) {
    if (!emailOrName) return 'Visitante';
    if (emailOrName.includes('@')) {
        if (window.mapUsuarios && window.mapUsuarios[emailOrName]) return window.mapUsuarios[emailOrName];
        const base = emailOrName.split('@')[0];
        return base.charAt(0).toUpperCase() + base.slice(1);
    }
    return emailOrName;
}
window.formatarNomeUsuario = formatarNomeUsuario;

export function formatarDataHora(timestamp) {
    if (!timestamp) return "";
    const data = new Date(timestamp);
    const dia = String(data.getDate()).padStart(2, '0');
    const mes = String(data.getMonth() + 1).padStart(2, '0');
    const hora = String(data.getHours()).padStart(2, '0');
    const min = String(data.getMinutes()).padStart(2, '0');
    return `${dia}/${mes} às ${hora}:${min}`;
}
window.formatarDataHora = formatarDataHora;

export function mostrarToast(msg, cor, icone) {
    const toast = document.getElementById('toast');
    if (!toast) return;
    document.getElementById('toast-msg').innerText = msg;
    document.getElementById('toast-icon').innerHTML = icone || '';
    toast.style.background = cor || 'rgba(245, 130, 32, 0.9)';
    toast.classList.add('mostrar');
    setTimeout(() => toast.classList.remove('mostrar'), 3000);
}
window.mostrarToast = mostrarToast;

export function aplicarTamanhosFonte(chatFs, codeFs) {
    document.documentElement.style.setProperty('--chat-fs', chatFs + 'rem');
    document.documentElement.style.setProperty('--code-fs', codeFs + 'rem');
}
window.aplicarTamanhosFonte = aplicarTamanhosFonte;

export function atualizarIndicadorApiKey(userApiKey) {
    const btn = document.getElementById('config-btn-apikey');
    if (btn) {
        if (userApiKey) {
            btn.innerHTML = `<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 2l-2 2m-7.61 7.61a5.5 5.5 0 1 1-7.778 7.778 5.5 5.5 0 0 1 7.777-7.777zm0 0L15.5 7.5m0 0l3 3L22 7l-3-3m-3.5 3.5L19 4"></path></svg> Chave Própria (Ativa) <span class="pulse-verde" style="background:#2ea043; width:8px; height:8px; border-radius:50%; display:inline-block; margin-left:5px;"></span>`;
            btn.style.color = "#2ea043";
        } else {
            btn.innerHTML = `<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 2l-2 2m-7.61 7.61a5.5 5.5 0 1 1-7.778 7.778 5.5 5.5 0 0 1 7.777-7.777zm0 0L15.5 7.5m0 0l3 3L22 7l-3-3m-3.5 3.5L19 4"></path></svg> Inserir Própria API Key`;
            btn.style.color = "#F58220";
        }
    }
}
window.atualizarIndicadorApiKey = atualizarIndicadorApiKey;

export function redimensionarEComprimirImagem(file, maxSize, callback) {
    const reader = new FileReader();
    reader.onload = function(e) {
        const img = new Image();
        img.onload = function() {
            let width = img.width; let height = img.height;
            if (width > maxSize || height > maxSize) {
                if (width > height) { height = Math.round((height * maxSize) / width); width = maxSize; } 
                else { width = Math.round((width * maxSize) / height); height = maxSize; }
            }
            const canvas = document.createElement('canvas'); canvas.width = width; canvas.height = height;
            const ctx = canvas.getContext('2d'); ctx.drawImage(img, 0, 0, width, height);
            callback(canvas.toDataURL('image/jpeg', 0.8), 'image/jpeg');
        }
        img.src = e.target.result;
    }
    reader.readAsDataURL(file);
}

// ==========================================================
// PARSERS DE CÓDIGO E GERENCIADOR CENTRAL DE DOWNLOADS
// ==========================================================
export async function salvarArquivoNativo(blob, sugeridoNome) {
    const configAskToSave = localStorage.getItem('unity_config_ask_save') !== 'false';
    if (configAskToSave && window.showSaveFilePicker) { 
        try { 
            const handle = await window.showSaveFilePicker({ suggestedName: sugeridoNome }); 
            const writable = await handle.createWritable(); 
            await writable.write(blob); 
            await writable.close(); 
            mostrarToast(getMeme('sucesso'), 'rgba(245, 130, 32, 0.9)', SVG_SAVE); 
            return; 
        } 
        catch (e) { return; } 
    } 
    const a = document.createElement('a'); 
    const url = URL.createObjectURL(blob);
    a.href = url; 
    a.download = sugeridoNome; 
    document.body.appendChild(a);
    a.click(); 
    document.body.removeChild(a);
    URL.revokeObjectURL(url);
    mostrarToast(getMeme('sucesso'), 'rgba(245, 130, 32, 0.9)', SVG_DOWNLOAD); 
}
window.salvarArquivoNativo = salvarArquivoNativo;

export function formatarBlocosDeCodigo() {
    document.querySelectorAll('.bot pre').forEach(pre => {
        if (pre.parentElement.classList.contains('code-wrapper')) return;
        const codeElement = pre.querySelector('code'); let linguagem = 'Code'; let linguagemRaw = '';
        if (codeElement && codeElement.className) { const match = codeElement.className.match(/language-(\w+)/); if (match) { linguagem = match[1]; linguagemRaw = match[1].toLowerCase(); } }
        if (codeElement && !codeElement.dataset.highlighted) hljs.highlightElement(codeElement);
        const wrapper = document.createElement('div'); wrapper.className = 'code-wrapper';
        
        let btnComentarios = '';
        if (['csharp', 'cs', 'javascript', 'js', 'cpp', 'java'].includes(linguagemRaw)) {
            btnComentarios = `<button class="btn-copy" onclick="window.toggleComentarios(this)">${SVG_EYE} Ocultar Comentários</button>`;
        }

        const header = document.createElement('div'); header.className = 'code-header'; 
        header.innerHTML = `<span>${linguagem}</span><div class="code-header-actions">${btnComentarios}<button class="btn-copy" onclick="window.baixarCodigo(this.parentElement.parentElement.nextElementSibling.innerText, '${linguagemRaw}')">${SVG_DOWNLOAD} Baixar</button><button class="btn-copy" onclick="window.copiar(this, this.parentElement.parentElement.nextElementSibling.innerText)">${SVG_COPY} Copiar</button></div>`;
        
        pre.parentNode.insertBefore(wrapper, pre); wrapper.appendChild(header); wrapper.appendChild(pre);
    });
}
window.formatarBlocosDeCodigo = formatarBlocosDeCodigo;

export function toggleComentarios(btn) {
    const wrapper = btn.closest('.code-wrapper');
    const code = wrapper.querySelector('code');
    if (code.classList.contains('hide-comments')) {
        code.classList.remove('hide-comments');
        btn.innerHTML = `${SVG_EYE} Ocultar Comentários`;
    } else {
        code.classList.add('hide-comments');
        btn.innerHTML = `${SVG_EYE_OFF} Mostrar Comentários`;
    }
}
window.toggleComentarios = toggleComentarios;

export function copiar(btn, texto) { 
    navigator.clipboard.writeText(texto).then(() => { 
        btn.innerHTML = `${SVG_CHECK} Copiado`; btn.classList.add('copiado'); 
        mostrarToast(getMeme('sucesso'), 'rgba(245, 130, 32, 0.9)', SVG_CHECK); 
        setTimeout(() => { btn.innerHTML = `${SVG_COPY} Copiar`; btn.classList.remove('copiado'); }, 2000); 
    }); 
}
window.copiar = copiar;

export async function baixarCodigo(texto, linguagem) {
    const ext = { 'csharp':'cs', 'cs':'cs', 'javascript':'js', 'js':'js', 'python':'py', 'html':'html', 'css':'css', 'json':'json', 'cpp':'cpp' }[linguagem] || 'txt';
    let nome = `script.${ext}`; const match = texto.match(/(?:class|interface|struct|enum)\s+([A-Za-z0-9_]+)/); if(match) nome = `${match[1]}.${ext}`;
    await salvarArquivoNativo(new Blob([texto]), nome);
}
window.baixarCodigo = baixarCodigo;

export function gerarMarkdownDaConversa(nomeProj, conv) { 
    return `# Projeto: ${nomeProj}\n## Conversa: ${conv.nome}\n\n---\n\n` + conv.mensagens.map(m => {
        if (m.papel === 'aluno') {
            const nomeAutor = m.autorEmail ? window.formatarNomeUsuario(m.autorEmail) : (m.autor || 'Colaborador');
            return `**🧑‍💻 ${nomeAutor}:**\n${m.texto}\n\n---\n\n`;
        } else {
            return `**🤖 ComboBoy:**\n${m.texto}\n\n---\n\n`;
        }
    }).join(''); 
}
window.gerarMarkdownDaConversa = gerarMarkdownDaConversa;

export async function exportarConversaMD() { 
    const proj = window.projetos[window.alvoMenu.indexProj]; const conv = proj.conversas[window.alvoMenu.indexConv]; 
    const mdTexto = gerarMarkdownDaConversa(proj.nome, conv);
    const nomeArquivo = `Unity_${conv.nome.replace(/[^a-z0-9]/gi, '_')}.md`;
    await salvarArquivoNativo(new Blob([mdTexto]), nomeArquivo);
}
window.exportarConversaMD = exportarConversaMD;

export async function exportarProjetoZip() { 
    const proj = window.projetos[window.alvoMenu.indexProj]; const zip = new JSZip(); 
    proj.conversas.forEach((c, i) => zip.file(`${c.nome.replace(/[^a-z0-9]/gi, '_')}_${i}.md`, gerarMarkdownDaConversa(proj.nome, c))); 
    const blob = await zip.generateAsync({type:"blob"}); 
    const nomeArquivo = `Proj_${proj.nome.replace(/[^a-z0-9]/gi, '_')}.zip`;
    await salvarArquivoNativo(blob, nomeArquivo);
}
window.exportarProjetoZip = exportarProjetoZip;